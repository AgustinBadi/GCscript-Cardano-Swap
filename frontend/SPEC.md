The provided open.html already has the calls of the gcscript api, integrates gc by the official library.

Rules:
- Bear in mind the comments.
- ALWAYS apply required modifications/updates ONLY on the DEVELOPER CUSTOMIZATION SECTION
- YOU ARE NOT ALLOWED to modify code under the ADVANCED AUTOGENERATED RUNTIME
- avoid introducing new code without first trying to extend or update currently existing code
- avoid introducing new code without first learning and iterating on current design patterns
- keep html without inlined css, and add id's and classes to properly reference them on the CSS block
- keep style consistent with a modern, neon style web3 dapp, with color pallette using of cyan/blue/violet/magenta and gradients of those in that order. 
- keep style ready for the dark and light themes
- reuse css class definitions as much as possible before introducing new classes.
- Keep it a minimal UI.